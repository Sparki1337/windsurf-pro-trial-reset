# 🤖 GitHub Actions Workflows

Этот проект использует GitHub Actions для автоматической сборки и тестирования на всех платформах.

## 📋 Workflows

### 🏗️ build.yml
**Автоматическая сборка для Windows, macOS и Linux**

**Триггеры:**
- Push в `main` или `develop`
- Pull requests
- Создание тегов `v*`
- Ручной запуск

**Что делает:**
1. Устанавливает Python 3.11 на каждой платформе
2. Устанавливает зависимости (PyQt6, Nuitka)
3. Собирает приложение с помощью Nuitka
4. Создаёт артефакты:
   - `WindsurfResetTool-Windows.exe` для Windows
   - `WindsurfResetTool-macOS.app.zip` для macOS
   - `WindsurfResetTool-Linux.bin` для Linux
5. При создании тега — создаёт GitHub Release

### 🧪 test.yml
**Тестирование на разных версиях Python и ОС**

**Триггеры:**
- Push в `main` или `develop`
- Pull requests
- Ручной запуск

**Матрица тестирования:**
- **ОС**: Windows, macOS, Linux
- **Python**: 3.9, 3.10, 3.11, 3.12

**Что делает:**
1. Проверяет импорты (PyQt6)
2. Проверяет синтаксис всех Python файлов
3. Убеждается, что код работает на всех платформах

## 🚀 Как использовать

### Создать релиз

```bash
# 1. Убедитесь, что все изменения закоммичены
git add .
git commit -m "Release v1.4.0"

# 2. Создайте тег
git tag v1.4.0

# 3. Отправьте тег на GitHub
git push origin v1.4.0

# 4. Подождите 10-15 минут
# GitHub Actions автоматически:
# - Соберёт приложение для всех платформ
# - Создаст релиз на странице Releases
# - Прикрепит все файлы к релизу
```

### Скачать артефакты сборки

1. Перейдите в **Actions** на GitHub
2. Выберите последний успешный запуск **Build Multi-Platform**
3. Прокрутите вниз до секции **Artifacts**
4. Скачайте нужный файл

### Ручной запуск workflow

1. Перейдите в **Actions** на GitHub
2. Выберите **Build Multi-Platform** или **Tests**
3. Нажмите **Run workflow**
4. Выберите ветку и нажмите **Run workflow**

## 📊 Badge статуса

Добавьте в README.md:

```markdown
![Build Status](https://github.com/YOUR_USERNAME/windsurf-pro-trial-reset/actions/workflows/build.yml/badge.svg)
![Tests Status](https://github.com/YOUR_USERNAME/windsurf-pro-trial-reset/actions/workflows/test.yml/badge.svg)
```

## 🔧 Настройка

### Secrets (не требуются)
GitHub Actions использует встроенный `GITHUB_TOKEN`, дополнительные secrets не нужны.

### Permissions
Убедитесь, что в настройках репозитория включены:
- **Settings** → **Actions** → **General** → **Workflow permissions**
  - ✅ Read and write permissions
  - ✅ Allow GitHub Actions to create and approve pull requests

## 📝 Подробности

См. [BUILD_INSTRUCTIONS.md](BUILD_INSTRUCTIONS.md) для полных инструкций по сборке.

---

© 2025 Sparki

